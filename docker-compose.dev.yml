services:
  ha-hearth-dev:
    container_name: ha-hearth-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      # Mount source code for hot-reload
      - .:/app
      # Prevent overwriting container's node_modules
      - /app/node_modules
      # Keep generated SvelteKit files aligned with the container's dependencies
      - /app/.svelte-kit
    networks:
      - ha-hearth-network
    ports:
      - '${DEV_PORT:-5173}:5173'
    environment:
      TZ: ${TZ:-UTC}
      HASS_URL: ${HASS_URL}
      HASS_PUBLIC_URL: ${HASS_PUBLIC_URL:-}
      NODE_ENV: development
    restart: unless-stopped
    # Development doesn't need health checks as it's monitored interactively

networks:
  ha-hearth-network:
    driver: bridge
